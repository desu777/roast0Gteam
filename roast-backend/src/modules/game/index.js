const GameService = require('./game.service');
const GameController = require('./game.controller');
const createGameRoutes = require('./game.routes');

module.exports = {
  GameService,
  GameController,
  createGameRoutes
}; 